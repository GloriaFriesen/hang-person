apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "hang-person"
version = '1.0'
mainClassName = "Game"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
